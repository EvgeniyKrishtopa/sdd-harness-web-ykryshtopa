---
type: regex
pattern: 'review-gates\.md.{0,300}(\bseven\b|семь)|(\bseven\b|семь).{0,300}review-gates\.md'
flags: 'is'
match: contains
---
And the file carrying the truth it drifted from, with its count. A drift
report that names only one of the two sides is not actionable — the reader
cannot tell which number is the wrong one.

This gate deliberately does not follow the CONFIRMED/PLAUSIBLE pause rule
(see `skills/harness-review/SKILL.md`), so this case grades on the two
claims the finding puts side by side, not on a verdict word.
